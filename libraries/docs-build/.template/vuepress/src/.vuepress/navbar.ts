import { navbar, type NavbarOptions } from 'vuepress-theme-hope'

/**
 * Icon 从 https://fontawesome.com/search 中获取
 */

const navbarOptions: NavbarOptions = {{ JSON.stringify(navbarOptions, undefined, 2) }}

export default navbar(navbarOptions)
